#include "monty.h"
/**
 * stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void thestack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifo = 0;
}
